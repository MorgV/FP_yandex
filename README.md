# Домашняя работа по функциональному программированию (часть 1 и 2)

## 📌 Общий подход

Использовал **чистые функции**, **каррирование**, **композицию** (`pipe`) и **декларативный стиль**.  
Для построения логики применялась библиотека **[Ramda](https://ramdajs.com/)**.

---

## ✅ Использованные принципы:

- **Каррирование** — функции частично применяются заранее (`api.get(...)`, `propEq(...)`);
- **Композиция** (`pipe`) — позволяет строить читаемые цепочки действий;
- **Чистые функции** — без побочных эффектов (кроме явных вызовов `writeLog`, `handleSuccess`, `handleError`);
- **Декларативный стиль** — код легко читать, он описывает «что», а не «как»;
- **Иммутабельность** — нигде не происходит мутация данных.

---

## 🧩 Часть 1: Валидации цветов фигур

### Цель:

Построить логические правила валидации полей на основе цвета геометрических фигур.

### Использованные функции из Ramda:

- `allPass`, `any` — проверка всех/одной функции-предиката
- `pipe` — композиция функций
- `equals`, `prop`, `propEq` — извлечение и сравнение значений
- `countBy`, `identity`, `toPairs` — подсчёт и работа со статистикой
- `complement` — инвертирование условий
- `filter`, `length`, `gte` — фильтрация и подсчёт

### Вспомогательные функции:

- `getFigureColors` — получение массива всех цветов фигур
- `countColorOccurrences` — количество фигур определённого цвета
- `isFigureColor` / `isNotFigureColor` — проверка цвета конкретной фигуры
- `areFiguresSameColor` — проверка, одинаковый ли цвет у двух фигур
- `allFiguresHaveColor` — проверка, что все фигуры нужного цвета

---

## 🔁 Часть 2: Обработка строки и цепочка API-вызовов

### Логика:

1. Логирую входную строку через `writeLog`
2. Проверяю входное значение (валидация: длина, положительность, формат)
3. Преобразую строку в число (`parseFloat`), округляю (`Math.round`)
4. Через API `/numbers/base` перевожу число в двоичную систему
5. Далее:
   - Логирую результат
   - Считаю длину строки
   - Возвожу в квадрат
   - Беру остаток от деления на 3
   - Использую результат как `id` для получения животного (`/animals.tech/{id}`)

### Использованные функции из Ramda:

- `pipe` — построение цепочек
- `tap` — логгирование промежуточных значений (с побочным эффектом)
- `allPass`, `test`, `gt`, `lt`, `length` — валидация строки

### Асинхронные действия:

- `api.get(...)({ ... })` — каррированная форма запроса
- `.then(...)` — обработка результата
- `.catch(...)` — обработка ошибок (в `handleError`)

---

## Домашняя работа по теме "Функциональное программирование"

Для создания проекта использовался create react app.

Версия Node — v14.17.1

В рамках этой домашней работы вам необходимо выполнить два задания используя приемы каррирования/композиции/частичного применения.
Чтобы прочитать подробные условия домашнего задания запустите проект, и откройте [localhost:3000](http://localhost:3000/).

### `npm install`

### `npm run start`

### Полезные ссылки

- [Документация Lodash](https://lodash.com/docs/4.17.15)
- [Документация Ramda](https://ramdajs.com/docs/)
- [Код с практикума](https://github.com/MoonW1nd/fp-live-coding)
